class Driver {
    constructor(name, date) {
        this.name = name
        this.date = date
    } 

    startDate() {
        let dateObj = new Date(this.date) 
    } 

    yearsExperienceFromBeginningOf () {
        startYr = getFulYear(dateObj) 
        
    }
}
